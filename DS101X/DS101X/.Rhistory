     ylab = "Counts of Insects")
plot(count ~ spray, data = InsectSprays,      xlab = "Types of Insecticide",      ylab = "Counts of Insects")
plot(count ~ spray, data = InsectSprays,      xlab = "Types of Insecticide",      ylab = "Counts of Insects") anova(lm(count ~ spray, data = InsectSprays))
InsectSprays
plot(count ~ spray, data = InsectSprays,      xlab = "Types of Insecticide",      ylab = "Counts of Insects") anova(lm(count ~ spray, data = InsectSprays))
strsplit("All happy families are alike; each unhappy family is unhappy in its own way", " ")
install.packages("tm")
library(tm)
txt <- tm_map(txt, stripWhitespace)
txt <- c("All happy families are alike; each unhappy family is unhappy in its own way") txt <- tm_map(txt, stripWhitespace)
txt <- tm_map(txt, content_transformer(tolower))
docs <- c("All happy families are alike; each unhappy family is unhappy in its own way") VCorpus(VectorSource(docs))
docs <- c("All happy families are alike; each unhappy family is unhappy in its own way") txt <- VCorpus(VectorSource(docs))
inspect(txt)
docs <- c("All happy families are alike; each unhappy family is unhappy in its own way") reuters <- VCorpus(VectorSource(docs)) inspect(reuters)
reuters <- tm_map(reuters, stripWhitespace)
reuters <- tm_map(reuters, content_transformer(tolower))
reuters <- tm_map(reuters, removeWords, stopwords("english"))
tm_map(reuters, stemDocument)
install.packages("SnowballC")
docs <- c("All happy families are alike; each unhappy family is unhappy in its own way") reuters <- VCorpus(VectorSource(docs)) inspect(reuters) reuters <- tm_map(reuters, stripWhitespace) reuters <- tm_map(reuters, content_transformer(tolower)) reuters <- tm_map(reuters, removeWords, stopwords("english")) tm_map(reuters, stemDocument)
tm_map(reuters, stemDocument)
docs <- c("All happy families are alike; each unhappy family is unhappy in its own way") reuters <- VCorpus(VectorSource(docs)) inspect(reuters) reuters <- tm_map(reuters, stripWhitespace) reuters <- tm_map(reuters, content_transformer(tolower)) reuters <- tm_map(reuters, removeWords, stopwords("english")) reuters <- tm_map(reuters, stemDocument)
reuters
inspect(reuters)
writeLines(as.character(reuters))
docs <- c("All happy families are alike; each unhappy family is unhappy in its own way") reuters <- VCorpus(VectorSource(docs)) inspect(reuters)
reuters <- tm_map(reuters, stripWhitespace) writeLines(as.character(reuters))
reuters <- tm_map(reuters, content_transformer(tolower)) writeLines(as.character(reuters))
reuters <- tm_map(reuters, removeWords, stopwords("english")) writeLines(as.character(reuters))
reuters <- tm_map(reuters, stemDocument) writeLines(as.character(reuters))
x <- c(-1, 0, 1, 2, 23) mean(x)
median(x)
install.packages("rstan", repos = "https://cloud.r-project.org/", dependencies = TRUE)
library(rstan)
fx <- inline::cxxfunction(signature(x = "integer", y = "numeric"), '     return ScalarReal( INTEGER(x)[0] * REAL(y)[0] ) ; ') fx(2L, 5)
library(rstan)
library(ggplot2)
install.packages("ggplot2")
library(ggplot2)
library(rstan)
install.packages("rstan", repos = "https://cloud.r-project.org/", dependencies = TRUE)
library(rstan)
library(ggplot2)
install.packages("ggplot2")
library(rstan)
install.packages("colorspace")
library(rstan)
fx <- inline::cxxfunction(signature(x = "integer", y = "numeric"), '     return ScalarReal( INTEGER(x)[0] * REAL(y)[0] ) ; ') fx(2L, 5)
remove.packages(rstan)
remove.packages("rstan")
