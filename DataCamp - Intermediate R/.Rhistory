x <- 3 if (x < 0) {     print("x is a negative number") }
x <- 3 if (x < 0) {     print("x is a negative number") }
x <- -3 if (x < 0) {     print("x is a negative number") }
x <- -3 if (x < 0) {     print("x is a negative number") } else {     print("x is 0 or is a positive number") }
x <- -3 if (x < 0) {     print("x is a negative number") } else if (x == 0 ){     print("x is zero") } else {     print("x is a positive number") }
c <- 1 while (c <= 7) {     print(paste("c has the value ", c))     c <- c + 1 }
c <- 1 while (c <= 7) {     print(paste("c has the value", c))     c <- c + 1 }
cities <- c("Toronto", "Vancouver", "Calgary", "Edmonton", "Quebec") for (city in cities) {     if (ncahr(city) == 7) {         print(city)     } }
cities <- c("Toronto", "Vancouver", "Calgary", "Edmonton", "Quebec") for (city in cities) {     if (nchar(city) == 7) {         print(city)     } }
for (i in length(cities)) {     print(cities[i]) }
for (i in 1:length(cities)) {     print(cities[i]) }
rquote <- "R's internals are irrefutably intriguing"
chars <- strsplit(rquote, split = "")[[1]]
chars
strsplit(rquote, split = "")
rquote <- "R's internals are irrefutably intriguing"
chars <- strsplit(rquote, split = "")#[[1]]
chars
rcount <- 0
for (c in chars) {
    if (c == "u") {
        break
    }
    if (c == "R" | c == "r") {
        rcount <- rcount + 1
    }
}
# Print the resulting rcount variable to the console
rcount
rquote <- "R's internals are irrefutably intriguing"
chars <- strsplit(rquote, split = "")[[1]]
chars
# Your solution here
rcount <- 0
for (c in chars) {
    if (c == "u") {
        break
    }
    if (c == "R" | c == "r") {
        rcount <- rcount + 1
    }
}
# Print the resulting rcount variable to the console
rcount
rquote <- "R's internals are irrefutably intriguing"
chars <- strsplit(rquote, split = "")[[1]]
chars
rquote <- "R's internals are irrefutably intriguing"
chars <- strsplit(rquote, split = "")#[[1]]
chars
rquote <- "R's internals are irrefutably intriguing"
charsrcount <- 0
for (c in chars) {
    if (c == "u") {
        break
    }
    if (c == "R" | c == "r") {
        rcount <- rcount + 1
    }
}
# Print the resulting rcount variable to the console
rcount <- strsplit(rquote, split = "")#[[1]]
chars
rquote <- "R's internals are irrefutably intriguing"
chars <- strsplit(rquote, split = "")#[[1]]
chars
chars <- strsplit(rquote, split = "")[[1]]
chars
# Pre-defined variables
rquote <- "r's internals are irrefutably intriguing"
chars <- strsplit(rquote, split = "")[[1]]
# Initialize rcount
rcount <- 0
# Finish the for loop
for (char in chars) {
    if (char == "u") {
        break
    }
    if (char == "R" | char == "r") {
        rcount <- rcount + 1
    }
}
# Print out rcount
rcount
